#include <iostream>

using namespace std;

int main() {
    int x = 6;
    int y = 5;
    if ( x > 5 ) {
        if ( y < 0 ) {
            cout << "snap";
        } else {
            cout << "crackle";
        }
    }

    return 0;
}
